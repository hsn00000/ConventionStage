{% extends 'base.html.twig' %}

{% block title %}Accueil Conventio - Bienvenue{% endblock %}

{# Assurez-vous que le CSS de Bootstrap est inclus dans base.html.twig #}

{% block content %}
    <main class="container-fluid my-5">

        {# Utilisation de la classe 'row' de Bootstrap.
           'align-items-center' centre le contenu verticalement.
           'h-100' est une classe personnalisée pour garantir la pleine hauteur.
        #}
        <div class="row two-column-layout align-items-center justify-content-center h-100">

            {# Colonne de Contenu (50% sur grand écran) #}
            {# 'order-2 order-lg-1' : Place cette colonne en seconde sur mobile, mais en première sur grand écran #}
            <div class="col-12 col-lg-6 content-side order-2 order-lg-1">
                <div class="styled-card shadow-lg p-4 p-md-5 mx-auto">
                    <h1 class="card-title">Bienvenue sur Conventio 👋</h1>
                    <p class="card-subtitle">Simplifiez la gestion de vos Conventions de Stage en quelques clics.</p>

                    <p class="description">
                        Que vous soyez étudiant, professeur ou responsable d'entreprise,
                        notre plateforme centralise tous les documents et le suivi nécessaire
                        pour des stages réussis.
                    </p>

                    {# Bouton d'appel à l'action principal (classe Bootstrap : btn) #}
                    <a href="/register/student" class="btn btn-primary cta-button">Commencer maintenant</a>
                </div>
            </div>

            {# Colonne du Logo/Image (50% sur grand écran) #}
            {# 'order-1 order-lg-2' : Place cette colonne en première sur mobile, mais en seconde sur grand écran #}
            <div class="col-12 col-lg-6 logo-side order-1 order-lg-2 text-center">
                {# 'logo-wrapper' garde le même rôle, mais le centrage est géré par la classe Bootstrap 'text-center' #}
                <div class="logo-wrapper d-flex justify-content-center align-items-center">
                    <img src="{{ asset('img/Conventio.png') }}" alt="Logo de Conventio" class="main-illustration img-fluid">
                </div>
            </div>

        </div>
    </main>
{% endblock %}
